<Window x:Class="Wpf_OnlineRestaurantSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="350" Width="525">
    <Grid>
        <!-- Controale pentru Login -->
        <TextBox x:Name="EmailTextBox" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Height="25" Margin="150,100,0,0" Text="{Binding Email}" />
        <PasswordBox x:Name="PasswordBox" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Height="25" Margin="150,150,0,0" PasswordChanged="PasswordBox_PasswordChanged" />
        <Button Content="Login" HorizontalAlignment="Left" VerticalAlignment="Top" Width="100" Height="30" Margin="200,200,0,0" Command="{Binding LoginCommand}" />

        <!-- Mesajul de eroare/succes -->
        <TextBlock Text="{Binding Message}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,250,0,0" Foreground="Red"/>
    </Grid>
</Window>
