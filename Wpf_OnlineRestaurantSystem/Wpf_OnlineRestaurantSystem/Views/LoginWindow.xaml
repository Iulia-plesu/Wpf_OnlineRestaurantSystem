<Window x:Class="Wpf_OnlineRestaurantSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="350" Width="525" WindowStartupLocation="CenterScreen">
    <Grid>

        <!-- Etichetă Email -->
        <TextBlock Text="Email:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="100,75,0,0" FontSize="14"/>

        <!-- Casetă text Email -->
        <TextBox x:Name="EmailTextBox" 
                 HorizontalAlignment="Left" 
                 VerticalAlignment="Top" 
                 Width="200" Height="25" 
                 Margin="150,70,0,0" 
                 Text="{Binding Email}" />

        <!-- Etichetă Parolă -->
        <TextBlock Text="Parolă:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="100,125,0,0" FontSize="14"/>

        <!-- Casetă parolă -->
        <PasswordBox x:Name="PasswordBox" 
                     HorizontalAlignment="Left" 
                     VerticalAlignment="Top" 
                     Width="200" Height="25" 
                     Margin="150,120,0,0" 
                     PasswordChanged="PasswordBox_PasswordChanged" />

        <!-- Buton Login -->
        <Button Content="Login" 
                HorizontalAlignment="Left" 
                VerticalAlignment="Top" 
                Width="100" Height="30" 
                Margin="200,180,0,0" 
                Command="{Binding LoginCommand}" />

        <!-- Mesaj eroare/succes -->
        <TextBlock Text="{Binding Message}" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Top" 
                   Margin="0,230,0,0" 
                   Foreground="Red"/>
    </Grid>
</Window>
