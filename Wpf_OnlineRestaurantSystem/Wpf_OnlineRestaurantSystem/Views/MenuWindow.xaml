<Window x:Class="Wpf_OnlineRestaurantSystem.Views.MenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_OnlineRestaurantSystem.Views"
        mc:Ignorable="d"
        Title="MenuWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Lista categorii -->
        <ComboBox 
            Grid.Column="0"
            ItemsSource="{Binding Categories}"
            SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
            DisplayMemberPath="Name"
            Width="200" Margin="10"/>

        <!-- Lista itemuri din categorie -->
        <ListBox Grid.Column="1"
             ItemsSource="{Binding MenuItems}" 
             SelectedItem="{Binding SelectedItem}"
             DisplayMemberPath="Name"
             Margin="10"/>

        <!-- Detalii sau subitemuri -->
        <StackPanel Grid.Column="2" Margin="10">
            <TextBlock Text="Detalii:" FontWeight="Bold" Margin="0,0,0,10"/>

            <!-- Detalii despre itemul selectat -->
            <ListView ItemsSource="{Binding SelectedItem}" Margin="10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                        <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Price, StringFormat=C}" Width="70"/>
                        <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" Width="250"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <!-- Sub-itemuri dacă este meniu -->
            <ItemsControl ItemsSource="{Binding SubItems}" Margin="10">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Description}" FontStyle="Italic"/>
                            <TextBlock Text="{Binding Price, StringFormat=C}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</Window>
