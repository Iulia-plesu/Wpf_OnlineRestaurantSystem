<Window x:Class="Wpf_OnlineRestaurantSystem.Views.OrderStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Order Status" Height="400" Width="600">
    
    <Grid Margin="10">
        <ListView ItemsSource="{Binding Orders}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="{Binding OrderId, StringFormat='Order ID: {0}'}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Status, StringFormat='Status: {0}'}"/>
                            <TextBlock Text="{Binding OrderDate, StringFormat='Date: {0:dd MMM yyyy HH:mm}'}" />
                            <TextBlock Text="{Binding TotalAmount, StringFormat='Total: {0:C}'}"/>
                            <TextBlock Text="Items:" FontWeight="Bold" Margin="0,5,0,0"/>
                            <ItemsControl ItemsSource="{Binding Items}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" Margin="10,0,0,0" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>